---
import CityPage from '../../components/CityPage.astro';
import { serviceAreas } from '../../data/service-areas';

const area = serviceAreas.find((a) => a.slug === 'miramar')!;
---

<CityPage area={area} />
